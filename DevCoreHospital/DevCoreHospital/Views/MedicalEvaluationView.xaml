<UserControl
    x:Class="DevCoreHospital.Views.MedicalEvaluationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:DevCoreHospital.Models"
    mc:Ignorable="d">

    <Grid Padding="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Spacing="15" Grid.Column="0" Margin="0,0,20,0">
            <TextBlock Text="New Diagnosis" FontSize="20" FontWeight="Bold"/>
            <TextBox Header="Symptoms" Text="{x:Bind ViewModel.Symptoms, Mode=TwoWay}" AcceptsReturn="True" MinHeight="80"/>
            <Button Content="Generate &amp; Save" Command="{x:Bind ViewModel.SaveDiagnosisCommand}" HorizontalAlignment="Right" Style="{StaticResource AccentButtonStyle}"/>
        </StackPanel>

        <StackPanel Grid.Column="1" Spacing="10">
            <TextBlock Text="Past Evaluations" FontSize="20" FontWeight="Bold"/>

            <ListView ItemsSource="{x:Bind ViewModel.PastEvaluations}" 
                      Background="{ThemeResource LayerFillColorDefaultBrush}"
                      CornerRadius="8" Padding="10">

                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:MedicalEvaluation">
                        <Grid Padding="10" BorderBrush="Gray" BorderThickness="0,0,0,1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,20,0">
                                <TextBlock Text="{x:Bind FormattedDate}" FontWeight="Bold"/>
                                <TextBlock Text="{x:Bind FormattedTime}" Foreground="Gray" FontSize="12"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{x:Bind Symptoms}" TextTrimming="CharacterEllipsis" MaxLines="1"/>
                                <TextBlock Text="Patient ID: System Generated" FontSize="11" Foreground="DeepSkyBlue"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</UserControl>