<Page
    x:Class="DevCoreHospital.Views.MedicalEvaluationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Medical Evaluation"
                   FontSize="28"
                   FontWeight="SemiBold" />

        <TextBlock Grid.Row="1"
                   Text="Evaluate patient symptoms, diagnosis, treatment and notes."
                   Foreground="#6B7280" />

        <ScrollViewer Grid.Row="2">
            <StackPanel Spacing="10">

                <TextBlock Text="Evaluator" />
                <TextBox Text="{Binding Evaluator, Mode=TwoWay}" PlaceholderText="Doctor name" />

                <TextBlock Text="Symptoms" />
                <TextBox Text="{Binding Symptoms, Mode=TwoWay}"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         MinHeight="80"
                         PlaceholderText="Describe symptoms..." />

                <TextBlock Text="Diagnosis Result" />
                <TextBox Text="{Binding DiagnosisResult, Mode=TwoWay}"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         MinHeight="80"
                         PlaceholderText="Diagnosis..." />

                <TextBlock Text="Meds List" />
                <TextBox Text="{Binding MedsList, Mode=TwoWay}"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         MinHeight="80"
                         PlaceholderText="Medication list..." />

                <TextBlock Text="Doctor Notes" />
                <TextBox Text="{Binding DoctorNotes, Mode=TwoWay}"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         MinHeight="100"
                         PlaceholderText="Additional notes..." />
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="10" HorizontalAlignment="Right">
            <Button Content="Clear" Command="{Binding ClearCommand}" />
            <Button Content="Save Evaluation" Command="{Binding SaveCommand}" />
        </StackPanel>
    </Grid>
</Page>